<div aria-hidden="true" aria-labelledby="changeStatutGESTGYMLabel{{ adherent.id }}" class="modal fade"
     id="changeStatutGESTGYM{{ adherent.id }}" role="dialog" tabindex="-1">
    <div class="modal-dialog" role="document">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title" id="changeStatutGESTGYMLabel{{ adherent.id }}">Changer le
                    statut</h5>
                <button aria-label="Close" class="close" data-dismiss="modal" type="button">
                    <span aria-hidden="true">&times;</span>
                </button>
            </div>
            <form action="{{ path('admin_editStatusGESTGYM', {id: adherent.id}) }}">
                <div class="modal-body">
                    <select class="form-control" id="status" name="status">
                        <option {% if adherent.isRegisteredInGestGym == true %} selected {% endif %}
                                value="1">Effectuée
                        </option>
                        <option {% if adherent.isRegisteredInGestGym == false %} selected {% endif %}
                                value="0">Non effectuée
                        </option>
                    </select>

                </div>
                <div class="modal-footer">
                    <button class="btn btn-secondary" data-dismiss="modal" data-toggle="modal"
                            type="button">
                        Annuler
                    </button>
                    <input class="btn btn-success" type="submit" value="Valider">

                </div>
            </form>
        </div>
    </div>
</div>
