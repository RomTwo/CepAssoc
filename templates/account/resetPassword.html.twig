{% extends 'base.html.twig' %}

{% block title %}Réinitialisation de votre mot de passe{% endblock %}

{% block body %}

    <h1 class="h3 mb-3 font-weight-normal">Changement de votre mot de passe</h1>

    {% if error is defined %}
        {{ error }}
    {% endif %}

    <form class="form-inline" action="{{ path('account_reset_password', {'token' : token } ) }}" method="post">
        <input class="form-control" type="password" name="password1" placeholder="Mot de passe" required>
        <input class="form-control" type="password" name="password2" placeholder="Confirmer votre Mot de passe"
               required>
        <input class="btn btn-success" type="submit" value="Valider">
    </form>


{% endblock body %}
